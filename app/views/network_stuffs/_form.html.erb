<%= form_with(model: network_stuff) do |form| %>
  <% if network_stuff.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(network_stuff.errors.count, "error") %> prohibited this network_stuff from being saved:</h2>

      <ul>
        <% network_stuff.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
